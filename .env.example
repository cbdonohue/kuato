# Required. Shared password that gates every page and API route.
SITE_PASSWORD=

# Optional. Enables Ask, Scout, Compare, roster review, news briefing,
# sleepers/fades, and the on-the-clock coach note. OpenAI is used when
# both keys are set. Without a key, the top-5 and reasons still work.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
